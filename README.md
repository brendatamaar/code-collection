# code-collection

Beta prerelease: `0.5.0-beta`. Expect breakage while parser coverage, CLI flags, and emitter edge cases are still being hardened.

A CLI tool that analyzes a backend repository and emits API collection files.

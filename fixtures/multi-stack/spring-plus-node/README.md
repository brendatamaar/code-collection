# Spring + Node BFF Fixture

Exercises beta multi-stack detection and IR merging:

- Spring backend under `backend/`
- Express-style Node BFF under `bff/`
- `--stack auto` should detect both stacks and merge endpoints from both parsers
